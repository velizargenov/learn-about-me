<% include _header %>

<% if ((currentUser) && (currentUser.username === user.username)) { %>
  <a href="/edit" class="pull-right">Edit your profile</a>
<% } %>

<h1><%= user.username %></h1>
<h2>Joined on <%= user.created_at %></h2>
<% if (user.bio) { %>
  <p><%= user.bio %></p>
<% } %>

<% include _footer %>
